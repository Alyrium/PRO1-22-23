#include <iostream>
using namespace std;

int main (){
    int n, c = 0;
    cin >> n;
    
    for (int i = 1, a = n; a>0; ++i) {
        a = a/2;
        c = i;
    } cout << "Base 2: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/3;
        c = i;
    } cout << "Base 3: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/4;
        c = i;
    } cout << "Base 4: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/5;
        c = i;
    } cout << "Base 5: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/6;
        c = i;
    } cout << "Base 6: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/7;
        c = i;
    } cout << "Base 7: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/8;
        c = i;
    } cout << "Base 8: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/9;
        c = i;
    } cout << "Base 9: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/10;
        c = i;
    } cout << "Base 10: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/11;
        c = i;
    } cout << "Base 11: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/12;
        c = i;
    } cout << "Base 12: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/13;
        c = i;
    } cout << "Base 13: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/14;
        c = i;
    } cout << "Base 14: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/15;
        c = i;
    } cout << "Base 15: " << c << " cifras." << endl;
    for (int i = 1, a = n; a>0; ++i) {
        a = a/16;
        c = i;
    } cout << "Base 16: " << c << " cifras." << endl;
    
    return 0;
}

// copypaste for the wiiiiiiiiiiiiiiiiin